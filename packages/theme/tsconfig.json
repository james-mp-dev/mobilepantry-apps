{
  "extends": "../config/tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist", // Though not strictly necessary for theme as it's mainly JS/CSS config
    "rootDir": "src",
    "allowJs": false
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
